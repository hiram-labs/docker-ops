{
  "name": "docker-ops",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "docker-compose up -d",
    "stop": "docker-compose down",
    "cdn:client": "docker exec -it cdn /bin/sh --login",
    "preinstall": "npx only-allow yarn"
  }
}
